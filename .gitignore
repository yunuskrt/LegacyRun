# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# playwright mcp session artifacts
/.playwright-mcp

# python
__pycache__/

# parser output — regenerate with `npm run parse:raw`
/src/data/parsed

# pipeline data — kept out of the repo, still on disk and in history before 97ce336.
# The suites and scripts that read these are excluded in tsconfig.build.json.
/src/data/db
/src/data/raw
/src/data/rating/season_players.ts

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma
